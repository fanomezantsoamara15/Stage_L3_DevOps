# apiVersion: v1
# kind: Service
# metadata:
#   name: frontend
#   namespace: quiz-connect
# spec:
#   type: NodePort
#   ports:
#     - port: 80
#       targetPort: 80
#       nodePort: 30080
#   selector:
#     app: frontend
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: quiz-connect
  labels:
    app: frontend
    tier: frontend
spec:
  type: NodePort   # OK pour test / staging. En prod → Ingress ou LoadBalancer
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
