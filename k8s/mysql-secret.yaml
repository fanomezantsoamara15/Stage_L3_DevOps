apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: quiz-connect
type: Opaque
stringData:
  root-password: "REPLACE_WITH_ROOT_PASSWORD"  # replace with real value at deploy time (use kubectl create secret or CI)
